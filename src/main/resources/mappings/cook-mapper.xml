<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cookMapper">
  <resultMap type="CommunityVO" id="CommunityVO">
  	<result column="COM_SEQ" property="com_seq"/>
    <result column="TITLE" property="title"/>
    <result column="ID" property="id"/>
    <result column="CONTENT" property="content"/>
    <result column="REGDATE" property="regdate"/>
    <result column="HIT" property="hit"/>
    <result column="LIKES" property="likes"/>
    <result column="TYPE" property="type"/>
  </resultMap>
   <insert id="insert" parameterType="CookVO">
     insert INTO comm_cook(type, COM_SEQ, REGDATE, TYPE)
     values('3',(COMMMUNITY_SEQ.NEXTVAL) + 1,
      #{content}, SYSDATE, '0', '0')
  	</insert>
	<!-- 작성중.. 헷갈ㄹ려 -->
	<select id="CommInteriorVO" resultType="CommInteriorVO">
		SELECT *
		FROM COMMUNITY C, COMM_COOK CO
		WHERE C.COM_SEQ = CO.COM_SEQ;
	</select>
</mapper>